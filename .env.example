# Third-Party MCP API Keys
# Copy this file to .env and add your actual API keys

# Context7 MCP - AI-powered documentation and code examples
# Get your API key from: https://upstash.com/docs/context7
CONTEXT7_API_KEY=your_context7_api_key_here

# GitHub MCP - Repository operations, issues, PRs
# Create a Personal Access Token at: https://github.com/settings/tokens
# Required scopes: repo, workflow
GITHUB_PERSONAL_ACCESS_TOKEN=your_github_token_here

# Memory MCP - Persistent key-value storage (no API key required)
# This MCP runs locally and doesn't require credentials

# Playwright MCP - Browser automation (no API key required)
# This MCP runs locally and doesn't require credentials
